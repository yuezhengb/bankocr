# BankOCR third-party notices

The release must ship the exact dependency lock file used to build it and an
SBOM generated by `scripts/create_release_metadata.py`. The release owner must
verify and preserve the license text for every dependency before redistribution.

BankOCR does not redistribute Microsoft Windows fonts. A CJK font may be
provided only when the release owner has the right to distribute it.
